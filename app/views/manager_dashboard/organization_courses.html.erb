<div class="section">
  <div class="container">
    <div class="columns is-centered">
      <h1 class="title is-2">Organization Courses</h1>
  </div>
</div>

<div class="columns">
  <div class="column">
    <%= form_tag manager_dashboard_path, method: :get, class: "search-form" do  %>
      <%= text_field_tag :q, params[:q], class: "string required input search" %>
      <%= submit_tag "Search", class: "button is-info is-rounded"%>
    <% end %>
    <table id="current-tb" class="table is-bordered is-hoverable is-striped">
      <thead>
        <tr class="is-selected">
          <th><%= sortable "title" %></th>
          <th><%= "Access Status"%></th>
          <th><%= link_to "Raiting" %></th>
          <th><%= sortable "created_at", "Created"%></th>
        </tr>
      </thead>
       <tbody>
        <% @organization_courses.each do |course| %>
          <tr>
            <th><%= link_to course.title, course_path(course) %></th>
            <th><%= course.access_state %>
            <th><div class="review-rating" data-score="<%= course_raiting(course) %>"></div></th>
            <th><%= course.created_at.strftime("%B %e, %Y") %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<div class="section">
  <div class="container">
    <div class="columns is-centered">
      <%= will_paginate @organization_courses, renderer: BulmaPagination::Rails %>
    </div>
    </br>
    <%= link_to 'Back', manager_dashboard_path, { class: "button  is-small is-black"} %>
  </div>
</div>

