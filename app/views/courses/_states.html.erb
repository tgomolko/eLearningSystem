<% if @course.draft? %>
  <%= link_to "Publish", ready_course_path(@course), method: :patch, title: "ready", class: "dropdown-item" %>
<% else %>
  <%= link_to "Draft", draft_course_path(@course), method: :patch, title: "draft", class: "dropdown-item" %>
<% end %>
