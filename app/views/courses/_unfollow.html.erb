<%= form_for(current_user.relationships.find_by(followed_id: @course.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Finish Course", class: "button is-danger is-rounded is-large is-centered" %>
<% end %>
