<div class="dropdown is-right">

  <div class="dropdown-trigger">
    <button class="button is-primary" aria-haspopup="true" aria-controls="dropdown-menu">
      <span>Profile</span>
      <span class="icon is-small">
        <i class="fa fa-angle-down" aria-hidden="true"></i>
      </span>
    </button>
  </div>

  <div class="dropdown-menu" id="dropdown-menu" role="menu">
    <div class="dropdown-content">
      <p class="dropdown-item">
        signed in as <strong><%= current_user.name %></strong>
      </p>

      <% if current_user.organization %>
        <hr class="dropdown-divider">

        <p class="dropdown-item"> You are manager of: </br>
          <strong>
            <%= link_to current_user.organization.company_name, current_user.organization %>
          </strong>
        </p>
      <% end %>

      <hr class="dropdown-divider">

      <%= link_to "Change profile", edit_user_registration_path, class: "dropdown-item" %>

      <%= link_to "Messages", conversations_path, class: "dropdown-item"%>

      <hr class="dropdown-divider">
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item"%>
    </div>
  </div>

</div>
