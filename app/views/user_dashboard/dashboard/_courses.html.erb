<div class="dashboard">
  <h1 class="title is-3 is-board">Courses</h1>
  <% if @not_org_courses.any? %>

    <table class="table is-bordered is-hoverable is-striped is-board">
      <thead>
        <tr class="is-selected">
          <th>Course name</th>
          <th>Author</th>
        </tr>
      </thead>
       <tbody>

        <% @not_org_courses.each do |course| %>
          <tr>
            <th><%= link_to course.title, course_path(course) %></th>
            <th><%= course.user.name %></th>
          </tr>
        <% end %>

      </tbody>
      <tr>
        <td colspan="2">&nbsp;<%= link_to "show more", user_dashboard_courses_path, class: "button is-small is-link"%></td>
      </tr>
    </table>

  <% else %>

    <div class="empty"></div>

  <% end %>
</div>
